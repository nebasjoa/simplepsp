{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @simplepsp/gateway... run build --if-present"
  },
  "deploy": {
    "startCommand": "pnpm db:deploy && pnpm --filter @simplepsp/gateway start",
    "restartPolicyType": "ON_FAILURE"
  }
}
