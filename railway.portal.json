{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @simplepsp/portal... run build --if-present"
  },
  "deploy": {
    "startCommand": "pnpm --filter @simplepsp/portal start",
    "restartPolicyType": "ON_FAILURE"
  }
}
